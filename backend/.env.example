# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Railway/Render provides this automatically
DATABASE_URL=postgresql://user:password@host:5432/database

# ===========================================
# FRONTEND URL
# ===========================================
FRONTEND_URL=http://localhost:3001

# ===========================================
# GHL OAUTH CONFIGURATION
# ===========================================
GHL_CLIENT_ID=your-client-id-here
GHL_CLIENT_SECRET=your-client-secret-here
GHL_SHARED_SECRET=your-shared-secret-here
REDIRECT_URI=http://localhost:3000/auth/callback

# For GHL Custom Menu Link SSO
# These are the same as above, used when users click your app in GHL sidebar

# ===========================================
# ENCRYPTION KEY
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=generate-64-character-hex-string

# ===========================================
# JWT AUTHENTICATION SECRETS
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate-64-character-hex-string-for-jwt
JWT_REFRESH_SECRET=generate-64-character-hex-string-for-refresh-token

# ===========================================
# STRIPE CONFIGURATION
# ===========================================
STRIPE_SECRET_KEY=sk_test_your-test-key
STRIPE_PRICE_ID=price_your-price-id
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ===========================================
# SENTRY (OPTIONAL ERROR TRACKING)
# ===========================================
SENTRY_DSN=https://your-key@sentry.io/your-project

# ===========================================
# REDIS CACHE (OPTIONAL - ROUND 2)
# ===========================================
# If not set, falls back to in-memory cache
# Railway/Upstash/Redis Cloud provide this
# Format: redis://default:password@host:port
REDIS_URL=

# ===========================================
# ROUND 2 FEATURE FLAGS (OPTIONAL)
# ===========================================

# Enable API documentation
ENABLE_API_DOCS=true

# Enable metrics endpoint (Prometheus)
ENABLE_METRICS=true

# Cache TTL override (seconds)
# CACHE_TTL_WORKFLOW=600
# CACHE_TTL_ANALYSIS=3600

# ===========================================
# EMAIL CONFIGURATION (SENDGRID - RECOMMENDED)
# ===========================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Free tier: 100 emails/day, Paid: Starting $19.95/month
SENDGRID_API_KEY=SG.xxxxx-your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=noreply@workflowdebugger.com
SENDGRID_FROM_NAME=WorkflowMD

# ===========================================
# AWS SES EMAIL CONFIGURATION (ALTERNATIVE)
# ===========================================
# Cost: $0.10 per 1,000 emails (vs SendGrid $20/month)
# See AWS_SES_SETUP.md for configuration instructions
# NOTE: If SENDGRID_API_KEY is set, SendGrid will be used instead

# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_REGION=us-east-1

# Email sender configuration
# SES_FROM_EMAIL=noreply@ghlworkflowdebugger.com
# SES_FROM_NAME=WorkflowMD

# ===========================================
# LEGACY SMTP (DEPRECATED - Use AWS SES above)
# ===========================================
# Old SMTP configuration (replaced by AWS SES)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password